# weather-app

## One-time setup

- Install Modules

```shell
npm install
```

- Create `.env` file

```shell
cp .env.default .env
# Adjust API_KEY in .env
```

- Bring up the UI server on port

```shell
npm run dev
```
